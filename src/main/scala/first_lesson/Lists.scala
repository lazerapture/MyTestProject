package first_lesson

import java.util.UUID


object Lists {
    def main(args: Array[String]) {
      val phones = List("Iphone1", "Iphone2", "Iphone3", "Iphone4", "Iphone5")

      println("model" + phones);
      println("The list of phones is empty:" + phones.isEmpty)
    }

}
